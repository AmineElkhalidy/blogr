import React from "react";

const Home: React.FC = () => {
  return (
    <React.Fragment>
      <section className="min-h-screen bg-gradient-to-r from-lightRed to-lighterRed"></section>
    </React.Fragment>
  );
};

export default Home;
